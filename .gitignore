node_modules/
dist/

# Entirely generated by copy-uv.js postinstall — do not commit.
# Vercel runs `npm install` (which triggers postinstall) before `vite build`,
# so these files will always exist by the time the build needs them.
public/uv/
