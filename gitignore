node_modules/
dist/

# Generated by postinstall — UV files are copied from node_modules at install time.
# Vercel runs `npm install` before build so this directory is created automatically.
public/uv/
